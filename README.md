# Ageless Recycling Calculator

A responsive, dependency-free estimator for container count, approximate weight, and payout. Rates remain visible and editable so the tool does not present old pricing as a guarantee.

Serve the directory from any static host. Run `npm test` and `npm run check` before deployment. Confirm public rates, accepted materials, contact details, and regulatory language with the business before launch.
